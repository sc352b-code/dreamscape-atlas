name: Inspect Hearthlands transfer payload v2

on:
  push:
    paths:
      - '.github/workflows/inspect-hearthlands-transfer-v2.yml'

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - name: Inspect share payload
        shell: bash
        run: |
          python - <<'PY'
          import urllib.request
          url='https://firestorage.ai/ja/f/xlFG0U8XX0Mc'
          html=urllib.request.urlopen(url).read().decode('utf-8','ignore')
          print(html)
          PY
